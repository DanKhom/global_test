#pragma once

#include <string>
#include <filesystem>
#include <iostream>
#include <filesystem>

extern bool IsFound;

void Processing_Directory(std::string target_filename, std::filesystem::path rootPath);
